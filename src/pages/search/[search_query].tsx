import React, { useEffect } from 'react';
// import api from 'api';
const SearchResult = () => {
  useEffect(() => {
    // api.getReviewByKeyword('광명').then((result) => {});
  }, []);
  return <div>후후</div>;
};

export default SearchResult;
